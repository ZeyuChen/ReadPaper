# ==============================================
# ReadPaper Configuration Template
# Copy this file to .env and fill in your values
# ==============================================

# ----------------------------------------------
# 1. Core Services (Required for BOTH Modes)
# ----------------------------------------------
# Get your key from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# ----------------------------------------------
# 2. Deployment Mode: LOCAL (Default)
# ----------------------------------------------
# Storage: Uses local disk (./paper_storage)
STORAGE_TYPE=local

# Frontend: Skip Google Login for faster local dev
NEXT_PUBLIC_DISABLE_AUTH=true
NEXT_PUBLIC_API_URL=http://localhost:8000

# ----------------------------------------------
# 3. Deployment Mode: CLOUD (Google Cloud)
# ----------------------------------------------
# Storage: Uses Google Cloud Storage
# STORAGE_TYPE=gcs
# GCS_BUCKET_NAME=your-unique-bucket-name
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Frontend: Real Google Authentication (Required for Cloud)
# NEXT_PUBLIC_DISABLE_AUTH=false
# AUTH_GOOGLE_ID=your_oauth_client_id
# AUTH_GOOGLE_SECRET=your_oauth_client_secret
# NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32
# NEXTAUTH_URL=https://your-frontend-url
