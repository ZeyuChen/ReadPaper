# ==============================================
# ReadPaper Configuration Template
# Copy this file to .env and fill in your values
# ==============================================

# ----------------------------------------------
# 1. Core Services (Required for ALL Modes)
# ----------------------------------------------
# Get your key from: https://aistudio.google.com/
GEMINI_API_KEY=your_gemini_api_key_here

# ----------------------------------------------
# 2. Deployment Mode: LOCAL (Default)
# ----------------------------------------------
# Storage: Uses local disk (Docker volume: paper_storage)
STORAGE_TYPE=local

# Auth: Skip Google Login for faster local dev
NEXT_PUBLIC_DISABLE_AUTH=true
DISABLE_AUTH=true

# NOTE: When using docker-compose, the frontend talks to the backend
# via internal Docker network. You do NOT need to change this for
# docker-compose deployments.
NEXT_PUBLIC_API_URL=http://localhost:8000

# ----------------------------------------------
# 3. Docker Compose Deployment (Self-hosted)
# ----------------------------------------------
# HTTP port (default: 80)
# HTTP_PORT=80

# For HTTPS: set your domain and email
# DOMAIN=yourdomain.com
# SSL_EMAIL=you@example.com

# After setting domain, update NEXTAUTH_URL and AUTH_URL:
# NEXTAUTH_URL=https://yourdomain.com
# AUTH_URL=https://yourdomain.com

# ----------------------------------------------
# 4. Authentication (Required for production)
# ----------------------------------------------
# Google OAuth credentials — get from Google Cloud Console
# https://console.cloud.google.com/apis/credentials
AUTH_GOOGLE_ID=your_google_oauth_client_id
AUTH_GOOGLE_SECRET=your_google_oauth_client_secret

# NextAuth secret — generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your_nextauth_secret_here

# NextAuth URL — set to your frontend domain (Cloud Run URL or custom domain)
# NEXTAUTH_URL=https://your-frontend-url

# Authorized redirect URIs (add these in Google Cloud Console):
#   Local:       http://localhost:3000/api/auth/callback/google
#   Docker HTTP: http://your-server-ip/api/auth/callback/google
#   HTTPS:       https://yourdomain.com/api/auth/callback/google

# ----------------------------------------------
# 5. Deployment Mode: CLOUD (Google Cloud Run)
# ----------------------------------------------
# Storage: Uses Google Cloud Storage
# STORAGE_TYPE=gcs
# GCS_BUCKET_NAME=your-unique-bucket-name
# GOOGLE_CLOUD_PROJECT=your-gcp-project-id

# Auth: Use real Google Authentication
# NEXT_PUBLIC_DISABLE_AUTH=false
# DISABLE_AUTH=false
